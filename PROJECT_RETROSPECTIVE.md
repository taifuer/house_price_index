# 全国 70 城商品住宅价格指数：项目工程复盘

> 复盘范围：2026-05-31 至 2026-07-31 期间从项目初始化到 `v2.0.0` 的主要设计、实现、修正和部署过程。<br>
> 文档日期：2026-08-01。<br>
> 当前维护分支：`dev`。<br>
> 项目仓库：[taifuer/house_price_index](https://github.com/taifuer/house_price_index)。<br>
> 线上站点：[house.taifua.com](https://house.taifua.com/)。

## 1. 结论

从仓库历史和完整协作过程看，这个项目确实是从零开始完成的一条端到端数据产品链路，而不只是做了几个图表。最终交付包含：

- 官方公开数据发现、抓取、历史页面解析和增量更新。
- 可追溯的长表数据资产和 gzip 压缩发布格式。
- 面向桌面端和移动端的 Streamlit 交互式看板。
- 城市排名、涨跌分布、城市层级对比、整体趋势、分层趋势和城市趋势。
- 缺失数据识别、覆盖度提示、来源原文链接和月份口径校准。
- 语法检查、聚合测试、抓取回归测试、应用冒烟测试和 GitHub Actions CI。
- Docker 镜像、Compose 编排、健康检查、日志轮转、Nginx 前置和生产环境配置隔离。
- `dev` 分支、`v2.0.0` Tag、GitHub Release 和线上站点的同提交发布。

当前数据资产包含 `168,904` 条记录、`160` 个有数据月份，覆盖 `2011-02` 至 `2026-06`。项目已经具备持续月度更新能力。

这里的“完整”不等于“没有边界”：早期历史数据仍有客观缺口，国家统计局页面和搜索接口仍属于外部依赖，月度更新目前也没有完全自动化。这些限制被明确暴露，而不是被图表掩盖。

## 2. 项目目标与约束

### 2.1 核心目标

核心问题始终是：如何把国家统计局每月发布的 70 城商品住宅销售价格指数，转换成可持续更新、可核验、可交互浏览的数据产品。

需求逐步收敛为：

1. 数据必须公开、权威、稳定，尽量使用国家统计局原始发布。
2. 支持历史月份和以后新增月份，不为每个月手工写 URL。
3. 保留城市、住宅类型、面积段、指标和来源页面等完整维度。
4. 默认视图应快速回答“哪些城市上涨、持平或下跌，市场整体和分层趋势如何”。
5. 桌面端保留必要分析能力，移动端优先保证滑动浏览不误触。
6. 在资源有限的服务器上长期运行，内存不能随访问次数无界增长。
7. 本地、GitHub 和生产服务器应能确认运行的是同一提交。

### 2.2 明确边界

当前版本有意不包含国际住宅价格指数和国际人口动态。它们的数据源本身质量较高，但与“全国 70 城商品住宅价格指数”主任务的关联不够直接，会增加页面长度、认知负担、数据口径解释和维护成本。

项目也没有引入数据库、独立 API 服务或复杂前端框架。以当前约 17 万行、月度更新一次的数据量，压缩 CSV、Pandas 和 Streamlit 足够完成任务。增加基础设施并不会自动提高数据质量。

## 3. 演进过程

### 3.1 第一阶段：从零建立数据和看板

2026-05-31 的初始提交一次建立了最小可用闭环：

- 国家统计局抓取脚本。
- 全历史 CSV 数据。
- Streamlit 看板。
- 城市排名、分布、层级对比和趋势视图。
- README、演示图和基础配置。

这一步的重要性在于先打通“来源页面到最终图表”，随后所有改进都能围绕真实数据和真实界面验证，而不是停留在原型。

### 3.2 第二阶段：让数据可以持续更新

2026-06-01 增加了 gzip 数据、读取回退和增量更新：

- 应用优先读取 `data/house_price_index_all.csv.gz`。
- 增量模式只处理当前最大月份之后的数据。
- 尚未出现在搜索结果中的月份写入 missing log。
- 不再根据日期猜测详情页 URL。

这使项目从一次性抓取脚本变成可以按月维护的数据管道。

### 3.3 第三阶段：探索上下文数据

项目曾实现两类补充数据：

- BIS 名义住宅价格指数，季度频率、`2010=100`。
- UN World Population Prospects 2024 国际人口动态，并使用中国国家统计局和德国 Destatis 最新官方值覆盖部分预测值。

这段探索证明了项目可以接入多来源公开数据，也暴露了“数据优质”不等于“应该放在同一页面”。经过多轮默认展开、默认折叠和布局调整后，2026-07-15 最终删除两类模块及其脚本和数据，让产品重新聚焦 70 城房价。

### 3.4 第四阶段：完善可读性、性能和兼容性

2026-06 至 2026-07 主要解决了：

- 移动端和桌面端默认视图差异。
- 趋势图例、缺失月份和数据覆盖提示。
- 视口探测造成的首次二次刷新。
- Streamlit 数据缓存上限和主数据内存占用。
- Plotly 控制台告警。
- pandas 2.x 分类数据兼容问题。
- 2026 年 6 月数据页“发布月份”和“数据月份”不一致的问题。

### 3.5 第五阶段：产品收敛和生产化

2026-07-15 后完成了：

- 整体趋势“总体 / 分层”切换。
- 默认近 10 年趋势和完整年份刻度。
- 概览中位数、来源原文入口和标题布局。
- 可调整侧边栏下的动态页头定位。
- Dockerfile、Compose、`.env`、CI 和部署文档统一入库。
- 桌面端与移动端 Plotly 交互分离。
- 移动端摘要四列网格。
- 城市排名响应式范围滑块。
- 生产服务器重建、健康检查和线上浏览器验收。

## 4. 最终架构

```text
国家统计局站内搜索后端
        |
        v
按数据月份组织候选详情页
        |
        v
抓取 HTML -> 文本 token 化 -> 表格识别 -> 城市和值解析
        |
        v
行数/城市数/月份口径校验
        |
        v
长表标准化 -> 唯一键去重合并 -> 确定性 gzip CSV
        |
        v
Streamlit + Pandas 聚合 + Plotly 图表
        |
        v
Docker Compose -> 127.0.0.1:8501 -> Nginx/TLS -> 公网站点
```

主要模块：

- `scripts/fetch_stats.py`：发现、抓取、解析、校验、合并和导出。
- `dashboard_trends.py`：总体趋势和城市层级趋势的可测试聚合逻辑。
- `app.py`：数据加载、筛选、图表、响应式 CSS 和必要的浏览器交互。
- `data/house_price_index_all.csv.gz`：应用直接读取的发布数据。
- `tests/`：抓取月份校准、趋势聚合和应用启动测试。
- `Dockerfile`、`docker-compose.yml`、`DEPLOY.md`：生产运行约定。
- `.github/workflows/ci.yml`：`dev` 分支和 PR 的持续集成。

## 5. 数据工程设计

### 5.1 数据源选择原则

同类项目不应只问“能不能抓到”，而应先评估：

| 维度 | 应回答的问题 |
| --- | --- |
| 权威性 | 是否为统计机构、监管机构或数据生产者直接发布？ |
| 稳定性 | 页面、API 或批量文件是否有相对稳定的发布方式？ |
| 时效性 | 发布频率、通常发布日期和延迟是多少？ |
| 历史覆盖 | 是否有足够长且口径可解释的时间序列？ |
| 完整性 | 实体、指标和分组是否完整，能否定义预期记录数？ |
| 机器可读性 | 是否有 API、CSV、Excel 或结构稳定的 HTML？ |
| 修订机制 | 历史值是否会修订，如何识别版本？ |
| 可追溯性 | 每条数据能否回到来源页面或数据集？ |
| 主题相关性 | 即使数据优质，是否真的帮助回答当前产品问题？ |

本项目最终选择国家统计局原文作为唯一看板数据源，搜索结果负责发现和月份口径，详情页负责数据值和来源追溯。

### 5.2 先发现，再抓取

最初最容易想到的方式是根据年月猜测 URL，但国家统计局 URL 包含发布时间和文章 ID，历史目录也经历过迁移。这种方式会产生大量无效请求，并且无法区分“尚未发布”和“猜错地址”。

最终方案使用国家统计局站内搜索所调用的政府搜索后端：

```text
https://api.so-gov.cn/query/s
```

请求中使用国家统计局 `siteCode`，从搜索结果标题提取数据月份，再收集详情页候选。它带来三个好处：

1. 只抓取搜索结果中真实存在的页面。
2. 一个数据月份存在多个迁移 URL 时，可以逐个尝试。
3. 搜索结果标题可以作为数据月份的外部校准依据。

这个接口不是项目控制的稳定契约，因此抓取逻辑仍保留直接 URL 模式和搜索页发现模式，不能把单个搜索后端视为永远不变。

### 5.3 候选 URL 和最佳解析结果

历史页面可能同时存在当前发布目录、旧信息公开目录和迁移页面。项目没有只保留第一个 URL，而是：

- 按月份收集多个候选。
- 优先当前规范目录。
- 逐个抓取和解析。
- 以记录数最多的候选作为当前最佳结果。
- 达到该月份预期记录数后提前结束尝试。

这种模式适用于所有存在历史迁移、重复发布或镜像页面的政府数据站点。关键是把“来源候选”和“解析质量”分开评价。

### 5.4 使用文本 token 而不是依赖单一 DOM 结构

国家统计局新旧页面的 HTML 表格结构并不统一。抓取器使用标准库 `HTMLParser`：

- 忽略 `script`、`style` 和 `noscript`。
- 收集正文文本 token、链接和图片地址。
- 通过标题、表号、城市名和数字序列识别数据，而不是绑定某个 CSS class。
- 支持“表”和“1”被拆成不同 token。
- 支持城市名在 HTML 中被拆成最多四段文本。
- 表 3、表 4 的 70 城可能分成“（一）”“（二）”两个 35 城区块。

这种解析比固定 XPath 更能兼容历史迁移页，但代价是必须有严格的领域校验，避免“解析出一些数字”就被误认为成功。

### 5.5 预期记录数是最重要的质量护栏

现代完整月份的记录数可以由业务结构推导。

非 1 月有三个指标：环比、同比、累计平均。

```text
表 1/2：70 城 x 3 指标 x 2 张表 = 420
表 3/4：70 城 x 3 面积段 x 3 指标 x 2 张表 = 1,260
总计：1,680
```

1 月没有累计平均列，只有环比和同比：

```text
表 1/2：70 城 x 2 指标 x 2 张表 = 280
表 3/4：70 城 x 3 面积段 x 2 指标 x 2 张表 = 840
总计：1,120
```

因此抓取器可以明确区分完整现代月份和部分解析。当前 160 个数据月份中，84 个达到对应预期记录数，76 个属于部分覆盖；最少月份有 140 条记录。早期缺表和失效迁移链接是来源事实，不能用插值伪装成完整数据。

### 5.6 数据月份不能只看页面发布日期

2026 年 6 月数据在 2026 年 7 月发布，详情 URL 和页面中的日期很容易让通用解析逻辑推断成 `2026-07`。最终规则是：

- 详情页负责数据值。
- 搜索结果标题中的“2026年6月份”负责预期数据月份。
- 两者不一致时，按搜索结果校准 `period` 和 `table_name`，同时输出 warning。

这类“观察期”和“发布日期”分离的问题普遍存在于统计公报、财报、月报和季度报告中，必须在数据模型中明确区分。

### 5.7 长表模型和幂等合并

当前发布字段为：

```text
period,table_no,table_name,house_type,size_band,city,metric,base,value,change_pct,source_url,title
```

记录唯一键为：

```text
period,table_no,house_type,size_band,city,metric
```

增量数据与现有数据按唯一键合并，新记录覆盖同键旧记录，再按固定业务顺序排序。这样重复运行不会不断追加重复行，也为以后处理官方修订保留了覆盖路径。

长表的价值在于：

- 筛选和分组逻辑统一。
- 新增指标或面积段不需要扩展大量列。
- 来源字段可以保留在每条记录上。
- Plotly Express、Pandas groupby 和导出都更直接。

### 5.8 增量更新不探测未发布页面

增量模式执行：

1. 读取现有 gzip 或 CSV。
2. 找到当前最大 `period`。
3. 通过搜索结果发现更晚月份。
4. 只抓取这些候选月份。
5. 从现有最大月份到上一个自然月计算理论应发布月份。
6. 未发现候选的月份写入 `data/house_price_index_missing.json`。

missing log 是运行诊断文件，不进入正式数据，也不提交 Git。这个设计把“没有发布”变成可观察状态，同时避免紧密轮询和猜测 URL。

### 5.9 压缩数据作为发布资产

当前 CSV 解压后为 `48,514,764` 字节，gzip 后为 `1,435,056` 字节，压缩率约 `97%`。文本中城市、标题、来源 URL 等重复值很多，因此 gzip 非常有效。

项目使用无时间戳的 gzip 输出，避免相同内容仅因压缩时间变化而产生不同二进制。应用仍保留未压缩 CSV 回退，方便本地调试。

对于当前规模，提交压缩 CSV 的优点是克隆后即可运行、部署不依赖抓取网络；缺点是 Git 对二进制月度变更不擅长增量审阅。数据继续增长后，可以再评估 GitHub Release 资产、对象存储或 Parquet，但现在没有必要提前复杂化。

## 6. 看板设计中有价值的功能

### 6.1 默认视图直接回答主要问题

首页默认最新月份、二手住宅、全部面积段、环比。这个默认值适合快速查看市场当月变化，用户仍可在侧边栏切换：

- 月份。
- 新建商品住宅或二手住宅。
- 全部或不同面积段。
- 环比、同比、累计平均。

“价格概览”标题右侧直接链接当前筛选数据的国家统计局原文，避免看板与来源脱节。

### 6.2 摘要指标兼顾数量和分布

概览同时展示：

- 覆盖城市。
- 上涨、持平、下降城市数。
- 均值。
- 中位数。
- 最小值到最大值区间。

中位数是后期补充的重要指标。70 城分布可能受极端城市影响，只看均值不足以描述典型城市。

### 6.3 排名、分布和分层是互补视角

城市排名回答“谁在前、谁在后”；首尾城市对比压缩极值信息；直方分布回答变化集中在哪些区间；城市层级对比回答一二三线城市是否出现结构性分化。

这些视图使用一致的语义配色：

- 红色表示上涨。
- 灰色表示持平或中性。
- 蓝色表示下跌。

整体趋势左上角增加图例后，不再要求用户从单月排名猜测颜色含义。

### 6.4 总体趋势和分层趋势使用不同分母

总体趋势使用上涨、持平、下跌城市数，纵轴正负对称，快速显示每月市场方向。

一二三线城市数量不同，分层趋势如果直接比较城市数会误导。因此分层模式以每层预期城市数为分母展示层内占比，并在 tooltip 中同时显示：

- 上涨、持平、下跌城市数。
- 实际覆盖城市数。
- 预期城市数。
- 数据是否完整。

这是“比较规模不同群组”时可复用的设计：总量视图使用绝对数，组间对比使用统一含义的比例，并保留原始计数。

### 6.5 缺失月份是数据的一部分

趋势聚合先构造从最早到最晚月份的完整月度索引，再回填真实观测。没有数据的月份覆盖数为零并标记“不完整”，而不是从横轴上消失。

城市趋势也构造“选中城市 x 完整月份”的网格，用于识别缺口。图中不伪造数据点，图下明确列出缺失月份范围。

这种处理能避免两类误读：

- 横轴压缩后，用户以为相邻两点只差一个月。
- 不完整月份仍被当作完整 70 城市场统计。

### 6.6 默认近 10 年，而不是全部历史

全部历史有研究价值，但首屏展示 15 年以上月度数据会挤压近期变化。最终整体趋势和城市趋势默认近 10 年，并提供全部、近 3 年、近 5 年、近 10 年按钮。

默认范围只是视图窗口，不裁剪数据，也不改变 tooltip 和缺失数据判断。

### 6.7 城市排名范围滑块

70 个城市在手机上不可能同时显示清楚，部分桌面宽度下也会拥挤。最终设计是：

- 桌面默认显示 30 城。
- 移动端默认显示 10 城。
- 下方范围滑块仍包含全部 70 城。
- 拖动选区平移可见窗口。
- 左右手柄单独调整窗口大小。
- 拖到边界时保持选区宽度，不让 Plotly 隐式改变窗口大小。
- 选区使用更明显的半透明蓝色填充和 `2px` 边框。
- 全部、一线、二线、三线切换时同步更新数据、坐标范围和滑块可见性。

这比让整个页面横向滚动更稳健：横向交互只发生在图表内部，不改变页面布局。

### 6.8 桌面和移动端采用不同交互策略

桌面端：

- 保留图内直接拖动缩放。
- 工具栏仅保留截图、重置和全屏等必要功能。
- 不展示容易造成状态残留的框选、拉索和多余 zoom 按钮。

移动端：

- 固定 Plotly 坐标轴，避免页面纵向滑动时误触缩放。
- 只保留截图和全屏。
- 通过专用范围滑块浏览城市排名。
- 趋势区间按钮和图例分行，避免窄屏重叠。

“响应式”不只是缩小字号，而是根据输入设备重新选择交互能力。

### 6.9 移动端摘要使用自定义网格

Streamlit 自动列布局在窄屏会把指标逐项堆叠，造成每行只有一个数字和大片空白。最终使用 CSS Grid：

- 第一行四列：覆盖城市、上涨、持平、下降。
- 第二行：均值、中位数、跨两列的区间。

稳定网格比依赖框架自动折行更适合固定数量的摘要指标。

### 6.10 折叠层级和长页面导航

“价格概览”和“价格趋势”使用 Streamlit expander，标题左侧沿用框架熟悉的展开图标，默认展开。早期曾尝试更显眼的自定义折叠形式，但会改变标题层级和占用额外空间，最终回到更克制的标题左侧图标。

页面右下角保留回到顶部和到底部按钮，适合趋势图较多的长页面。按钮不能固定压在 footer 上方某个猜测像素值，因此脚本测量 footer 与视口的实际重叠高度，写入 CSS 变量动态上移按钮。经验是：固定控件不仅要避开视口边缘，还要避开页面末尾会进入视口的内容。

## 7. 国际数据和人口数据探索的经验

### 7.1 第一版城市人口数据为什么没有继续使用

早期人口背景数据只有 `66/70` 城，最新年份停留在 2010 年，出生率、死亡率等指标也不完整。继续绘图会给用户一种“已有正式人口面板”的错觉，因此页面只显示数据不足说明，没有硬画图。

这个决定值得复用：数据质量达不到展示门槛时，空状态或明确说明优于漂亮但误导的图。

### 7.2 后续探索过的优质来源

国际人口模块曾使用：

- UN World Population Prospects 2024 compact 数据。
- `1950-2023` Estimates 历史估计。
- `2024` 以后 Medium variant 中位预测。
- 中国国家统计局 2024、2025 官方实绩覆盖。
- 德国 Destatis 2024 出生、死亡等官方实绩覆盖。

指标包括人口、出生人口、死亡人口、自然增长人口、净迁移人口、人口变化、出生率、死亡率和自然增长率。中国 2025 年出生人口 `792` 万也通过官方公报覆盖，而不是继续使用 UN 预测值。

国际住宅价格模块使用 BIS 公开批量文件：

```text
https://data.bis.org/static/bulk/WS_SPP_csv_flat.zip
```

选择名义住宅价格指数和 `2010=100` 统一口径，覆盖中国、美国、英国、德国、日本、韩国。

### 7.3 混合实际值、估计值和预测值的风险

人口模块的难点不是有没有数据，而是：

- UN Estimates、UN Medium variant 和各国官方实绩不是同一种数据状态。
- 各国“年末人口”“年中人口”“常住人口”口径可能不同。
- 出生、死亡、迁移之间存在统计调整，不能总是假设严格相加。
- 最新值发布时间不同，国家之间可比年份不一致。
- 不同国家规模差异巨大，共用纵轴会压扁日本、韩国等序列。

如果再次实现，应把 `series_type`、来源、口径和覆盖状态作为一等字段，并允许每个国家使用独立纵轴。更合理的产品形态是独立的“国际人口动态”页面，而不是附着在 70 城房价页面底部。

### 7.4 最终删除是产品优化，不是失败

BIS 和 UN WPP 都是优质来源，但页面主问题是中国 70 城房价。两类国际模块引入了两个抓取脚本、两套数据文件、大量 UI 和来源解释，却不能明显改善主任务。

2026-07-15 的范围收敛删除了两个共 `529` 行的数据脚本及相关数据和界面。得到的经验是：

> 数据质量、数据稳定性、主题相关性和维护成本必须同时满足。高质量但偏题的数据，应进入独立产品或暂不展示。

## 8. 性能和内存优化

### 8.1 Streamlit 约 500 MiB RSS 不等于内存泄漏

生产服务器曾观察到 Streamlit 进程 RSS 约 `512,348 KiB`，服务器总内存约 `3,654 MiB`。这包含：

- Python 解释器和已加载模块。
- Streamlit 运行时和会话管理。
- Pandas DataFrame。
- Plotly 图对象和序列化过程。
- Python 内存分配器已申请但暂未归还操作系统的页。
- 缓存内容。

判断泄漏不能只看单次 RSS，而应在固定请求负载后观察是否持续、不可回落地增长。项目没有把访问结果追加到全局列表，也没有为每次访问创建无上限缓存，因此普通访问次数不会直接线性累加主数据副本。

### 8.2 缓存必须有上限和失效键

主数据加载使用：

```python
@st.cache_data(max_entries=1)
```

缓存参数包含文件路径和 `mtime_ns`。文件更新后修改时间变化，会产生新缓存结果；`max_entries=1` 又能淘汰旧版本，避免每次月度更新都永久保留一份 DataFrame。

早期补充数据缓存也设置了有限条目数。补充模块删除后，主数据只需一个缓存项。

### 8.3 分类列显著降低 DataFrame 内存

当前数据含大量重复字符串，例如城市、住宅类型、面积段、指标、来源 URL 和标题。加载后将这些列转为 Pandas `category`。

在 2026-08-01 对当前 `168,904` 行数据的同口径测量中：

| 状态 | DataFrame 深度内存 |
| --- | ---: |
| 普通字符串列 | 约 `131.4 MiB` |
| 重复字符串列转为 `category` | 约 `15.0 MiB` |
| 降幅 | 约 `88.6%` |

这不代表整个 Streamlit 进程只占 15 MiB，但它移除了主数据中最明显的可控浪费。

### 8.4 分类类型也带来兼容性陷阱

内存优化后，城市层级对比曾报错：

```text
TypeError: Cannot setitem on a Categorical with a new category (99)
```

原因是对分类列 `map()` 后直接 `fillna(99)`，而 `99` 不在分类集合中。修复方式是先转为字符串，再映射排序值：

```python
tier_summary["city_tier"].astype("string").map(tier_rank).fillna(99).astype(int)
```

同时在 groupby 中使用 `observed=True`，只聚合实际出现的分类。经验是：`category` 不是无成本替换，排序、填充和 groupby 都需要回归测试。

## 9. 前端和交互踩坑

### 9.1 视口识别导致 PC 首次刷新两次

Streamlit 服务端初次渲染时不知道浏览器宽度。早期实现无论桌面还是移动端都追加 `viewport` 参数并 `replace()`，PC 首次访问也会重新加载一次。

最终约定桌面为无参数默认值，只在移动端首次访问或现有参数与宽度不一致时更新 URL。这样 PC 不再额外刷新，移动端最多进行一次必要校准。

通用经验：能作为服务端默认值的主流视口不要写回 URL，只为非默认状态增加参数。

### 9.2 侧边栏可调整宽度，但标题位置曾固定

只根据“展开 / 收起”设置固定标题偏移，无法覆盖用户拖动侧边栏宽度。最终用 `ResizeObserver` 和 `MutationObserver` 读取侧边栏实际宽度，写入 CSS 变量：

```text
--app-sidebar-width
```

页头标题和 footer 分隔线均根据该变量调整。动态布局应跟随实际几何尺寸，而不是猜测组件状态。

### 9.3 原文链接放进标题栏并不只是换一行代码

Streamlit expander 标题不是普通 Python 容器，直接输出链接只会出现在标题下方。项目最终在 DOM 渲染后定位当前 expander 的 `summary p`，把来源图标移动到标题文本右侧，并阻止点击图标触发折叠。

实现中有重试和清理逻辑，因为 Streamlit rerun 会重建 DOM。这个方案满足了界面要求，但也属于版本敏感代码，升级 Streamlit 后必须用浏览器回归验证。

### 9.4 CSS 看似修改成功，实际间距没有变化

多次顶部间距调整“刷新后效果不明显”，根因通常不是数值不够大，而是：

- Streamlit 生成了额外 element container。
- 注入 CSS/HTML 的辅助节点仍占据布局。
- 选择器优先级或移动端 media query 覆盖了桌面规则。
- header 固定定位后，内容区 padding 与首个 expander 自身 margin 同时生效。

最终通过无头浏览器截图和 DOM 几何测量，而不是继续盲调像素。辅助 style marker 被压缩为零宽高，顶部间距分别在桌面和移动端验证。

### 9.5 页面底部横向条并不一定是图表溢出

最后一次桌面端问题表现为：鼠标悬停 body 后，底部出现横向滚动条。测量显示：

- `documentElement`、`body`、`stMain` 的 `clientWidth` 与 `scrollWidth` 相等。
- Streamlit 主内容区默认 `overflow-x: auto`。
- footer 为保持全宽使用了基于视口宽度的分隔线。
- 正常浏览器存在纵向滚动条宽度，`100vw` 与内容可用宽度可能产生细小差异。

最终保留 footer 视觉宽度，仅把 `stMain` 横向滚动设为 hidden。排名图自己的范围滑块不依赖页面横向滚动，因此不受影响。

通用经验：先测量到底是页面、滚动容器、伪元素还是图表内部溢出，再决定在哪一层裁剪。直接给 `body` 全局隐藏可能掩盖真实布局错误。

### 9.6 移动端按钮和图例重叠

Plotly 的 update menu 和 legend 都默认占用图上方空间。窄屏下只改横坐标会发生重叠。最终移动端：

- 增加图表高度和上边距。
- 区间按钮放在更高的一行。
- 图例保持单独一行并缩小字号。

固定格式控件必须预留稳定空间，不能依靠 Plotly 自动避让。

### 9.7 框选、拉索和重置状态

桌面端曾开放 box select、lasso、zoom 等多个工具。问题包括：

- 普通用户难以判断当前处于哪种 drag mode。
- Reset axes 重置坐标后，选择状态和工具按钮状态可能仍保留。
- 移动端页面滚动容易误触。

曾尝试在点击 Reset axes 后清空 `selections` 和 `selectedpoints`，但最终产品判断是这些工具对当前看板价值有限，因此移除比维护复杂状态机更合理。桌面保留图内直接缩放和必要工具，移动端固定范围。

### 9.8 Plotly 控制台告警不能忽略

处理过两类告警：

```text
WARN: ignored xaxis2.matches: "x" to avoid an infinite loop
WARN: encountered bad format: "+.1f"
```

第一类来自 facet 子图自动轴匹配与手工配置冲突，修复为对子图坐标轴显式 `matches=None`。第二类来自 Plotly 6 对 hover 数值格式的解析差异，移除了不兼容的前置加号格式。

图能显示不代表配置正确。控制台告警往往预示升级后的行为变化，应该进入浏览器验收项。

### 9.9 范围滑块的边界行为需要额外约束

Plotly 原生范围滑块在把选区拖到最左或最右边缘时，可能改变选区宽度。需求则是：

- 拖动选区只平移。
- 只有左右手柄可以缩放。
- 到达边缘后停止，不继续压缩。

项目监听 pointer、mouse 和 touch 事件，在拖动结束后读取当前范围并按原宽度校正到合法边界；手柄事件被排除，因此仍可独立调整大小。Streamlit rerun 前会移除旧监听器，防止重复绑定。

这类浏览器交互不能只用鼠标验证，还需要真实 touch 事件和窄视口测试。

## 10. 测试与验证体系

### 10.1 当前自动化测试

当前共有 6 项 unittest：

- 2 项 Streamlit AppTest：无百度统计和有合法百度统计 ID 时均能启动。
- 3 项趋势聚合测试：补齐缺失月份、分层分母、部分覆盖标记。
- 1 项抓取回归测试：详情页推断月份与搜索月份不一致时进行校准。

CI 在 `dev` push 和面向 `dev` 的 pull request 上执行：

```bash
python -m py_compile scripts/fetch_stats.py dashboard_trends.py app.py
python -m unittest discover -s tests -v
```

### 10.2 数据更新验收

每次解析逻辑或月度数据更新至少应检查：

1. 一个现代完整月份。
2. 一个历史迁移页面。
3. 1 月两指标结构。
4. 非 1 月三指标结构。
5. 现代月份是否为 `1,120` 或 `1,680` 条。
6. 每张表覆盖城市数。
7. 最大 `period`、来源 URL 和标题是否一致。
8. 合并后唯一键是否重复。
9. gzip 输出可被应用直接读取。

### 10.3 浏览器验收

这次项目证明，仅靠 Python 测试不足以保证 Streamlit 页面正确。实际使用无头 Chromium 验证过：

- `320`、`390`、`820`、`1440` 等视口。
- 页面和主内容区 `clientWidth/scrollWidth`。
- 侧边栏展开、收起和调整宽度。
- 原文图标是否真正位于标题右侧。
- 移动端摘要是否四列排布。
- 趋势按钮和图例是否重叠。
- Plotly modebar 在不同设备上的按钮集合。
- 排名滑块的鼠标、pointer 和 touch 拖动。
- 选区到两侧边界时宽度是否保持。
- 左右手柄是否仍能调整范围。
- 页面控制台 error 和 warning。

截图必须与 DOM 数值一起看。截图能发现视觉问题，几何测量能区分真实溢出和滚动容器行为。

### 10.4 生产验收

发布完成后检查：

- 本地 `HEAD`。
- GitHub `origin/dev`。
- `v2.0.0^{}` 实际指向的 commit。
- 服务器仓库 `HEAD`。
- 容器是否基于新代码重建。
- `/_stcore/health` 是否返回 `ok`。
- 公网站点是否返回 HTTP 200。
- 容器日志是否有异常。
- 百度统计环境变量是否保留。
- 线上桌面端和移动端是否真的出现新样式。

### 10.5 仍然存在的测试缺口

当前测试数量仍偏少，特别是抓取器。后续优先补充：

- 城市名被拆分为多个 token 的 HTML fixture。
- 1 月两指标表格 fixture。
- 表 3/4 分成两个 35 城区块的 fixture。
- 缺少历史表格和失效候选 URL 的 fixture。
- 现代完整月份 1,680 条端到端解析测试。
- 唯一键重复、gzip 确定性和 missing log 测试。
- 将目前临时无头浏览器脚本整理为仓库内 Playwright 冒烟测试。

## 11. 部署、版本和协作经验

### 11.1 `git pull` 不会自动更新正在运行的容器

生产容器把 `app.py` 和数据 COPY 到镜像。服务器即使已经拉取最新代码，旧容器仍运行旧镜像。正确更新流程是：

```bash
git pull --ff-only origin dev
docker compose up -d --build --remove-orphans
```

然后再检查容器创建时间、健康状态和线上界面。对于 bake-in 数据的镜像，这一步尤其重要。

### 11.2 开发和生产只允许 `.env` 不同

Dockerfile、Compose 和应用代码都进入 Git。生产差异只保存在未提交的 `.env`：

- `BAIDU_ANALYTICS_ID`。
- 国内可用的 `PIP_INDEX_URL`。

百度统计 ID 在应用启动时校验为 32 位十六进制值。容器重建前后都核对环境变量，避免发布时意外丢失统计配置。

### 11.3 Docker 配置中的有效细节

- 基础镜像使用 `python:3.11-slim`。
- Python 依赖锁定明确版本。
- `pip install --no-cache-dir` 减少镜像层体积。
- 使用 UID `10001` 的非 root 用户运行应用。
- `init: true` 处理子进程和信号。
- `restart: unless-stopped` 保证异常后恢复。
- 只绑定 `127.0.0.1:8501`，由 Nginx 提供公网入口。
- 容器内置 Streamlit healthcheck。
- json-file 日志设置 `10m x 3` 轮转，避免磁盘无界增长。

### 11.4 Git 身份和 Co-Authored-By 必须在推送前检查

早期远程提交曾显示错误作者，需要设置项目级 Git 用户：

```text
taifu <taifu@taifua.com>
```

代理协作提交还要求独立 trailer：

```text
Co-Authored-By: Codex (GPT-5.6 Sol) <noreply@openai.com>
```

Trailer 必须是真实换行后的独立行，不能把 `\n` 字面量写进提交正文。推送前应使用 `git show` 和 `%(trailers)` 检查。

`AGENTS.md` 是仓库级开发和提交约定，属于应当版本化的项目文档，不应因为名称含 Agent 就自动加入 `.gitignore`。

### 11.5 Tag 不等于 GitHub Release

只推送 `v2.0.0` Tag 不会自动产生完整 GitHub Release Notes。需要使用 GitHub UI 或 `gh release create/edit` 创建和维护 Release。

本项目按当时明确要求多次把 `v2.0.0` 强制移动到最新提交，并同步覆盖 Release Notes。这能满足单版本演示期，但不应成为正式版本的长期惯例。更稳健的后续策略是：

- 已发布 Tag 保持不可变。
- 修复发布为 `v2.0.1`、`v2.0.2`。
- 功能版本发布为 `v2.1.0`。
- Release Notes 记录数据覆盖变化、功能变化和验证命令。

### 11.6 当前只维护 `dev`

项目中途将本地分支调整为 `dev`，并一度同步本地和远程的 `main/dev`。仓库后期明确只维护 `dev`，CI、服务器和 Release 的目标都统一为 `dev`。这避免本地 `main/dev`、远程 `main/dev` 和服务器分支长期漂移。

单分支可以满足个人项目；如果以后多人协作，应恢复短期 feature branch + PR + `dev` 的流程，而不是直接在生产分支上反复改写 Tag。

## 12. 可复用的数据产品方法

### 12.1 通用处理流水线

对其他稳定周期发布数据，可以复用以下十步：

1. **定义来源契约**：发布机构、频率、观察期、发布日期、指标口径和修订规则。
2. **发现可用发布**：通过官方目录、搜索 API、RSS、批量索引或数据目录发现，不猜资源 ID。
3. **保留候选来源**：历史迁移或重复发布时允许一个期间对应多个 URL。
4. **解析为领域记录**：利用实体名、表号、指标名等稳定语义，不只依赖页面样式。
5. **计算预期覆盖**：实体数 x 指标数 x 分组数，定义完整、部分和失败。
6. **标准化长表**：观察期、实体、指标、分组、单位/基期、值、来源 URL。
7. **按唯一键幂等合并**：重复运行不产生重复，官方修订可以覆盖。
8. **发布紧凑数据资产**：小规模用确定性 gzip CSV，大规模再考虑 Parquet 或对象存储。
9. **可视化暴露质量状态**：显示覆盖数、缺失月份、数据类型和原文入口。
10. **可重复部署和验收**：提交 SHA、CI、镜像、健康检查和线上浏览器结果一致。

### 12.2 推荐的通用长表字段

本项目字段可以抽象为：

```text
period
entity
indicator
subgroup
unit_or_base
value
source_url
source_title
```

新项目还可以增加：

```text
published_at
fetched_at
revision
series_type       # 官方实绩、估计、预测、派生
quality_status    # 完整、部分、异常
source_checksum
```

不要把“官方实绩”和“预测”仅写在图下注释里，它们应该进入数据模型。

### 12.3 每次周期更新的标准运行手册

```text
1. 检查当前最大观察期。
2. 从官方索引确认新一期已经发布。
3. 运行增量抓取，不猜 URL。
4. 检查记录数、实体数、表号和来源。
5. 检查观察期与发布日期是否混淆。
6. 检查唯一键和历史记录是否意外变化。
7. 生成确定性压缩数据。
8. 运行语法检查、单测和应用冒烟测试。
9. 用桌面和移动端截图检查默认视图。
10. 提交数据覆盖变化和来源说明。
11. 推送分支并等待 CI。
12. 发布新版本，生产环境拉取并重建镜像。
13. 核对 SHA、健康端点、日志和公网界面。
```

### 12.4 数据质量门槛

适合复制到新项目的检查清单：

- [ ] 来源是直接数据生产者或可信国际机构。
- [ ] 能区分观察期和发布日期。
- [ ] 有稳定实体列表或可解释的实体变化。
- [ ] 能计算每期预期记录数。
- [ ] 部分数据不会被当成完整统计。
- [ ] 实际值、估计值、预测值和派生值有明确标记。
- [ ] 每条记录可以回到来源页面。
- [ ] 增量更新幂等。
- [ ] 未发布与抓取失败是不同状态。
- [ ] 历史缺口不会被静默插值。
- [ ] 数据更新后缓存和容器会真正失效。
- [ ] 页面在移动端不会因图表交互阻塞正常滚动。

## 13. 最有价值的决策

按长期收益排序，本项目最值得保留的决策是：

1. **把来源链接放在当前筛选标题旁边**：核验成本最低。
2. **把完整性变成显式状态**：缺失月份和部分覆盖不再隐形。
3. **使用搜索发现而不是猜 URL**：减少无效请求和错误月份。
4. **用预期记录数验证解析结果**：把领域知识变成机器护栏。
5. **长表 + 唯一键幂等合并**：简化增量更新和多视图聚合。
6. **默认近 10 年但保留全量**：兼顾可读性和研究需求。
7. **总体绝对数 + 分层占比**：避免不同规模群组的误导比较。
8. **桌面和移动端采用不同交互策略**：解决真实输入设备问题。
9. **用浏览器测量而不是持续猜 CSS**：明显减少布局问题反复。
10. **删除偏题的优质数据模块**：范围收敛本身是一项产品能力。
11. **缓存上限 + category**：在不改变架构的情况下显著降低内存风险。
12. **代码、Docker 和生产配置统一入库**：减少“服务器上是另一个版本”。

## 14. 当前技术债和后续优先级

### P0：提高数据更新可靠性

- 为现代页、历史迁移页、1 月表格和拆分城市名增加真实 HTML fixture。
- 生成每月质量清单：记录数、表号、城市数、唯一键、来源 URL 和 warning。
- 将增量更新做成定时任务，但先生成 PR 或待审核数据，不直接无人值守发布。
- 明确官方历史修订策略，区分新增月份和旧月份修订。

### P1：降低前端维护风险

- 将 1,889 行的 `app.py` 拆为数据加载、图表构建、样式和浏览器增强模块。
- 把临时 Playwright 验收脚本整理为仓库内的最小 E2E 测试。
- 为 Streamlit DOM 选择器增加版本升级检查。
- 在页面或镜像中暴露 build commit，便于确认线上版本。

### P1：规范发布

- 后续停止覆盖 `v2.0.0`，采用不可变语义版本。
- Release Notes 自动带入数据最大月份、记录数、测试结果和 commit。
- 生产部署记录镜像 digest 和源提交。

### P2：可观测性和数据资产增强

- 定期记录进程 RSS、容器重启次数和健康检查失败。
- 视需要保存少量原始 HTML fixture 和来源 checksum，而不是保存全部原始页面。
- 数据规模明显增长后再评估 Parquet；当前无需数据库化。
- 国际住宅或人口数据若重新启用，建立独立页面和独立更新节奏。

## 15. 参考来源和技术资料

当前正式数据源：

- [国家统计局](https://www.stats.gov.cn/)
- [国家统计局站内搜索](https://www.stats.gov.cn/search/s)
- [国家统计局 2026 年 6 月 70 城发布页示例](https://www.stats.gov.cn/xxgk/sjfb/zxfb2020/202607/t20260715_1964115.html)

历史探索过但当前产品未使用：

- [BIS Data Portal](https://data.bis.org/)
- [UN World Population Prospects](https://population.un.org/wpp/)
- [德国联邦统计局 Destatis](https://www.destatis.de/EN/Home/_node.html)

主要技术组件：

- [Streamlit](https://streamlit.io/)
- [Plotly Python](https://plotly.com/python/)
- [Pandas](https://pandas.pydata.org/)
- [Docker Compose](https://docs.docker.com/compose/)
- [GitHub Actions](https://docs.github.com/actions)

仓库内相关文档：

- [README.md](./README.md)：安装、抓取、启动和功能说明。
- [DEPLOY.md](./DEPLOY.md)：Docker 生产部署步骤。
- [AGENTS.md](./AGENTS.md)：开发、测试和提交规范。

## 16. 最后的工程判断

这个项目最可复用的部分不是某一个 Plotly 图或某一段 Streamlit CSS，而是以下组合：

```text
权威来源
+ 可发现的发布索引
+ 能解释历史差异的解析器
+ 可计算的完整性标准
+ 幂等增量更新
+ 对缺口诚实的可视化
+ 设备相关的交互设计
+ 有上限的缓存和可量化的内存优化
+ 同提交的 CI、Release 和生产部署
```

只要周期性公开数据具备相对稳定的实体、指标和发布时间，这套方法可以迁移到其他统计公报、价格指数、人口、就业、贸易、金融或行业运行数据。迁移时最先复用的应该是数据质量和发布流程，而不是页面外观。
